package com.hcmus.forumus_client.ui.profile

/**
 * Display modes for profile content filtering.
 * GENERAL shows all content mixed, POSTS shows only posts, REPLIES shows only comments.
 */
enum class ProfileMode {
    GENERAL,
    POSTS,
    REPLIES
}
